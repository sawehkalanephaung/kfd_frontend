# Template environment variables for KFD Frontend
# Copy to `.env.local` for local development:
#   cp .env.example .env.local

# Backend API base URL
# Production backend:
NEXT_PUBLIC_API_URL=http://kfd-backend-prod.eba-vidrjgde.ap-southeast-1.elasticbeanstalk.com
# Local backend (when running kfd_backend locally on port 8080):
# NEXT_PUBLIC_API_URL=http://localhost:8080
